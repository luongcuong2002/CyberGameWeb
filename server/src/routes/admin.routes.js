const express = require('express');
const router = express.Router();

//const { authJwt } = require("../middleware");
// const controller = require("../controllers/admin.controller");

// router.get("/get-account-waiting", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.getAccountWaiting);
// router.post("/delete-account-waiting", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.deleteAccountWating);
// router.post("/allow-account-waiting", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.allowAccountWating);
// router.get("/allow-all-account-waiting", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.allowAllOfAccountWaiting);

// router.get("/get-all-account-possible", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.getAllAccountPossible);
// router.post("/add-account-possible", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.addAccountPossible);
// router.post("/delete-account-possible", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.deleteAccountPossible);
// router.post("/find-account-possible", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.findAccountPossible);

// router.post("/get-data-users-played", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.getDataUsersPlayed);
// router.post("/update-users-played", [authJwt.verifyToken, authJwt.isDuplicateAdminAndModerator], controller.updateUsersPlayed);

module.exports = router;